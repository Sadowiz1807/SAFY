# Phase 4 Source Consistency Matrix

| Topic | Source says | Phase 4 plan says | Status | Evidence | Action |
|---|---|---|---|---|---|
| Phase 4 scope | Phase 4 delivers Agent Core, Skills, Provider, SkillPolicy, `/agent/chat`, and create DB in sandbox | Plan covers those components and keeps implementation blocked until approval | CONSISTENT | `03_PHASE_PLAN.md`; `PHASE_4_PLAN.md` Scope/Required Order | Implement only after explicit approval |
| Create database gate | Create database prompt must produce sandbox schema and UI technical result | Create_database workflow returns sandbox schema and Safy-envelope UI technical result | CONSISTENT | `PHASE_4_PLAN.md` Create Database Target Workflow; `PHASE_4_CONTRACTS.md` response envelope | Future tests must assert envelope and technical_result |
| Connected DB execution | Agent connected DB query is not Phase 4 implementation scope | Connected DB agent execution, including read-only query, is out of scope for Phase 4 and deferred | DEFERRED_TO_PHASE_5 | `PHASE_4_PLAN.md` Non-Negotiable Boundaries; `PHASE_4_SECURITY_SPEC.md` P0 Rules | Keep Phase 4 tasks blocking connected DB reads/writes |
| Visualization/dashboard | Current phase plan does not assign visualization/dashboard to Phase 6 | Visualization/dashboard is out of scope for Phase 4 and is not assigned to Phase 6 unless later approved | NEEDS_DETAIL | User direction after review | Do not implement or assign to a phase until later user approval |
| SQL Guard | SQL parser/AST validation, identifier sanitizer, permission checker, risk analyzer, query limiter are guard requirements | ToolExecutor must call SQL Guard before every SQL execution | CONSISTENT | `SAFY_source.md` Guard section; `PHASE_4_SECURITY_SPEC.md` SQL Rules | Future ToolExecutor tests must prove no bypass |
| Multi-statement blocker | Existing guard blocks generic multi-statement SQL | Phase 4 allows only trusted sandbox create-database DDL after splitting and validating each statement | NEEDS_DETAIL | `SAFY_source.md` multi-statement blocker; `PHASE_4_SECURITY_SPEC.md` SQL Rules | Future implementation must define splitter/validator and keep non-sandbox blocker |
| SkillPolicy authority | Skills describe behavior but runtime policy enforces permissions | SkillPolicy is deterministic and includes allowed/denied tools, targets, sandbox-only flag, timeout, audit, redaction | CONSISTENT | `PHASE_4_CONTRACTS.md` Skill Contracts; `PHASE_4_SECURITY_SPEC.md` SkillPolicy Rules | Future compiler must fail closed if policy fields missing |
| Provider output trust | LLM/provider output cannot directly execute tools or SQL | Provider output is untrusted structured data validated by Agent Core and ToolExecutor | CONSISTENT | `PHASE_4_PLAN.md` Non-Negotiable Boundaries; `PHASE_4_CONTRACTS.md` Provider Contracts | Keep mock provider first and validate schemas |
| Safy envelope | API responses should follow standard envelope shape | `/agent/chat` response uses `success`, `data`, `error`, `meta`; `technical_result` is inside `data` | CONSISTENT | `PHASE_4_CONTRACTS.md` Response Envelope | Future API tests must assert envelope shape |
| UI compatibility | UI should receive technical result without Phase 4 UI rewrite | Plan returns renderable `workflow_id`, `workspace_id`, `summary`, `assumptions`, `risk_level`, `technical_result` | CONSISTENT | `PHASE_4_PLAN.md` UI Technical Result Compatibility | No Apps/Web changes in planning |
| Runtime/audit/provenance | System needs auditability and safe records | Plan records workflow, tool attempts, SQL Guard metadata, sandbox workspace, redacted provenance | CONSISTENT | `PHASE_4_PLAN.md` Runtime/Audit/Provenance Integration | Future schema changes require implementation approval |
| Raw secrets | Credentials/tokens/connection strings must not leak | Plan forbids raw secrets in provider prompts, API response, logs, audit, and provenance | CONSISTENT | `PHASE_4_SECURITY_SPEC.md`; `PHASE_4_CONTRACTS.md` no raw secret rule | Run redaction/secret tests during implementation |
| Task dispatch | Task board dispatch requires complete task fields and explicit dispatchability | Phase 4 task contracts are expanded but all remain `dispatchable: false` and `dispatchable_now: false` | CONSISTENT | `PHASE_4_TASKS.yaml` rules and tasks | Do not update dispatchability without user approval |
| Phase 0-3 behavior | Phase 0-3 PASS behavior must remain stable | Phase 4 planning preserves query safety boundaries and requires regression tests | CONSISTENT | `PHASE_3_FINAL_REPORT.md`; `PHASE_4_VALIDATION_CHECKLIST.md` | Future implementation must run Phase 1-3 regression |
