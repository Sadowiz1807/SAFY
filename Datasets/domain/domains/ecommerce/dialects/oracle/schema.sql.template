-- Oracle schema template
CREATE TABLE {{TABLE_NAME}} (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    {{COLUMN_NAME}} VARCHAR2(255) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT SYSTIMESTAMP NOT NULL
);
