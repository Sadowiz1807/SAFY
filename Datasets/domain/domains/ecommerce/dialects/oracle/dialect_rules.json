{
  "database_type": "oracle",
  "types": {
    "id": "NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY",
    "string": "VARCHAR2(255)",
    "datetime": "TIMESTAMP WITH TIME ZONE",
    "boolean": "NUMBER(1)",
    "json": "CLOB"
  },
  "pagination": "FETCH FIRST {n} ROWS ONLY",
  "notes": [
    "Use Oracle SQL.",
    "Do not assume BOOLEAN table columns.",
    "Use sequences only for compatibility cases where identity is unavailable."
  ],
  "required_validation": [
    "parse_with_target_dialect",
    "execute_on_disposable_target",
    "verify_schema_references",
    "verify_result_contract",
    "verify_risk_and_route_labels"
  ]
}