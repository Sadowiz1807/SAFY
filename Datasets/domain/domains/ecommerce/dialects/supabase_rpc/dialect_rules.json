{
  "database_type": "supabase_rpc",
  "types": {
    "id": "BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY",
    "string": "TEXT",
    "datetime": "TIMESTAMPTZ",
    "boolean": "BOOLEAN",
    "json": "JSONB"
  },
  "pagination": "LIMIT {n}",
  "notes": [
    "SQL dialect is PostgreSQL.",
    "Execution transport is Supabase PostgREST/RPC.",
    "Dataset labels must retain provider=supabase and execution_transport=postgrest_rpc.",
    "Do not include API keys or project secrets."
  ],
  "required_validation": [
    "parse_with_target_dialect",
    "execute_on_disposable_target",
    "verify_schema_references",
    "verify_result_contract",
    "verify_risk_and_route_labels"
  ]
}