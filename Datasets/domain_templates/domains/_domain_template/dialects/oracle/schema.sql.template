-- Placeholder schema for oracle; do not execute against production.
CREATE TABLE {{TABLE_NAME}} (
  id GENERATED BY DEFAULT AS IDENTITY,
  {{COLUMN_NAME}} VARCHAR2(255) NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE
);
