-- Placeholder schema for supabase_rpc; do not execute against production.
CREATE TABLE {{TABLE_NAME}} (
  id GENERATED BY DEFAULT AS IDENTITY,
  {{COLUMN_NAME}} TEXT NOT NULL,
  created_at TIMESTAMPTZ
);
