"""Incremental SAFY route ownership markers.

The legacy FastAPI app still registers compatibility routes in main.py. These modules document
and expose per-domain ownership so future migrations can include routers without changing URLs.
"""
